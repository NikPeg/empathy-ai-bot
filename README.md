# 🤖 Empathy AI Bot (Эдя)

Интеллектуальный Telegram-бот с искусственной эмпатией для эмоциональной поддержки и активного слушания. Использует языковые модели через OpenRouter API для ведения дружелюбных бесед, запоминает контекст и отправляет напоминания для поддержания диалога.

## 📋 Описание

**Эдя** — это чатбот, который:
- 💬 Ведет естественные диалоги с пользователями
- 🧠 Запоминает контекст предыдущих сообщений
- ⏰ Отправляет умные напоминания для продолжения общения
- 🎯 Активно интересуется жизнью пользователя
- 📊 Хранит историю диалогов в локальной базе данных
- 🔧 Поддерживает администраторские команды для управления

## ✨ Возможности

### Для пользователей:
- Естественное общение с AI-ассистентом
- Сохранение контекста беседы (до MAX_CONTEXT сообщений)
- Автоматические напоминания о продолжении диалога
- Возможность сбросить историю диалога
- Поддержка Markdown форматирования в ответах

### Для администраторов:
- Отправка сообщений конкретному пользователю
- Массовая рассылка всем пользователям
- Отслеживание активности пользователей
- Отладочный режим с пересылкой сообщений

## 🛠 Технологии

- **Python 3.14+**
- **aiogram 3.20** - асинхронный фреймворк для Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **aiohttp** - асинхронные HTTP-запросы к OpenRouter API
- **OpenRouter API** - доступ к различным LLM моделям
- **telegramify-markdown** - конвертация Markdown для Telegram

## 📦 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd empathy-ai-bot

# Настройте переменные окружения
cp .env.example .env
nano .env  # Заполните токены

# Запустите бота
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### Вариант 2: Локальный запуск

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd empathy-ai-bot

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/macOS
# venv\Scripts\activate   # Windows

# Установите зависимости
pip install -r requirements.txt

# Настройте переменные окружения
cp .env.example .env
nano .env  # Заполните токены

# Запустите бота
python main.py
```

### Обязательные параметры в `.env`:

- `TG_TOKEN` — токен от [@BotFather](https://t.me/BotFather)
- `LLM_TOKEN` — API ключ от [OpenRouter](https://openrouter.ai/)
- `MODEL` — модель LLM (например, `google/gemini-2.0-flash-exp:free`)
- `DEBUG_CHAT` — ваш chat ID от [@userinfobot](https://t.me/userinfobot)

📖 **[Полное руководство по развертыванию →](docs/deployment.md)**

## 📝 Команды бота

### Пользовательские команды:

- `/start` — приветствие и информация о боте
- `/help` — список доступных команд
- `/forget` — сбросить историю диалога
- `/reminder` — отключить напоминания

### Команды администратора:

- `/dispatch` — отправить сообщение конкретному пользователю
- `/dispatch_all` — отправить сообщение всем пользователям

## 🗂 Структура проекта

```
empathy-ai-bot/
├── docs/                   # Документация
│   └── deployment.md      # Полное руководство по развертыванию
├── data/                   # База данных и данные (создается автоматически)
├── main.py                 # Основная логика бота
├── openrouters.py          # Интеграция с OpenRouter API
├── user_db.py             # Работа с базой данных пользователей
├── prompts.json           # Системные промпты для LLM
├── messages.json          # Шаблоны сообщений бота
├── requirements.txt       # Зависимости Python
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
├── .dockerignore          # Исключения для Docker
├── kill_bot.sh            # Скрипт для остановки бота
├── .env                   # Переменные окружения (не в git)
├── .env.example           # Пример переменных окружения
├── .gitignore             # Игнорируемые файлы
└── README.md              # Документация
```

## 🗄 База данных

Бот использует SQLite для хранения данных пользователей:

### Структура таблицы `users`:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Telegram chat ID (PRIMARY KEY) |
| `name` | TEXT | Username пользователя |
| `prompt` | JSON | История диалога |
| `remind_of_yourself` | TEXT | Дата/время следующего напоминания |
| `sub_lvl` | INTEGER | Уровень подписки |
| `sub_id` | TEXT | ID подписки |
| `sub_period` | INTEGER | Период подписки |
| `is_admin` | INTEGER | Флаг администратора |

## ⚙️ Конфигурация

### Настройка поведения бота

- **`prompts.json`** — системные промпты для LLM (основной диалог и напоминания)
- **`messages.json`** — текстовые шаблоны сообщений бота

### Основные параметры в `.env`

```env
# Напоминания
DELAYED_REMINDERS_HOURS=2      # Напоминание через N часов
DELAYED_REMINDERS_MINUTES=0    # + дополнительные минуты
FROM_TIME=9                     # Начало временного окна (час)
TO_TIME=23                      # Конец временного окна (час)
TIMEZONE_OFFSET=3               # Часовой пояс (UTC+3 для МСК)

# Контекст и БД
MAX_CONTEXT=20                  # Количество сообщений в истории
DATABASE_NAME=users.db          # Имя файла базы данных
```

📖 **[Подробнее о конфигурации →](docs/deployment.md#настройка-переменных-окружения)**

## 📊 Логирование

- **Console** — информационные сообщения (INFO)
- **debug.log** — детальные логи (DEBUG) с автоматической ротацией

Просмотр логов:
```bash
# Локально
tail -f debug.log

# Docker
docker-compose logs -f
```

## 🛡 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл
- Храните токены в секрете
- Используйте разные токены для dev/prod
- Регулярно обновляйте зависимости

## ❗ Решение проблем

Основные проблемы и их решения:

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверьте `TG_TOKEN`, статус и логи |
| Ошибки LLM | Проверьте `LLM_TOKEN`, баланс и модель |
| Ошибки БД | Проверьте права доступа, пересоздайте БД |
| Бот не останавливается | Используйте `./kill_bot.sh` |

📖 **[Полное руководство по решению проблем →](docs/deployment.md#решение-проблем)**

## 🤖 Демо-версия

**Попробуйте бота прямо сейчас:** [@kindness365bot](https://t.me/kindness365bot)
