# Финальная работа: Внедрение LLM в бизнес-продукты
## Часть 4. Карта связей «Бизнес KPI → Продукт KPI → Модельные метрики»

---

## 📋 Задача

Построить иерархическую карту связей для проекта **Эдя (Empathy AI Bot)**:
1. Выписать 2-3 ключевых **бизнес-KPI**
2. Для каждого выбрать 2-3 **продукт-KPI**
3. Для каждого продукт-KPI подобрать 2-3 **модельных метрики**
4. Указать **как измеряем** каждую метрику

---

## 🎯 Бизнес-модель Эди

### **Текущая стадия**: MVP → Product-Market Fit (B2C)
### **Будущая стадия**: B2B масштабирование (Q2-Q3 2025)

### **Источники выручки** (roadmap):

#### **B2C (Индивидуальные пользователи)**:
1. **Freemium модель** (как Replika, ChatGPT Plus)
   - **Free tier**: Базовый функционал (текстовые диалоги, лимит контекста)
   - **PRO tier**: $7-10/месяц
     - Безлимитный контекст
     - Голосовые сообщения
     - Приоритетная поддержка
     - Персональные техники (КПТ упражнения)
     - Экспорт истории диалогов

2. **Партнерские интеграции**
   - Реклама медитационных приложений (Calm, Headspace)
   - Партнерство с онлайн-терапевтами (комиссия за рефералов)

#### **B2B (Корпоративные клиенты)**:
1. **SaaS подписка для компаний**
   - $5-10 за сотрудника в месяц
   - **Пример**: Яндекс (5000 сотрудников) × $7 × 12 мес = **$420,000/год**

2. **White-label решения**
   - Кастомизация под бренд компании
   - Интеграция с корпоративными системами (AD, Slack)
   - Setup fee: $10,000-50,000 + monthly subscription

3. **Партнерство с клиниками**
   - Лицензирование технологии
   - $500-1000/месяц за клинику

---

## 📈 Иерархия метрик: Обзор

```
УРОВЕНЬ 1: БИЗНЕС-KPI (Стратегические цели компании)
├── Выручка (Revenue)
├── Прибыльность (Profitability)
└── Рост (Growth)

УРОВЕНЬ 2: ПРОДУКТ-KPI (Пользовательский опыт и поведение)
├── Привлечение (Acquisition)
├── Активация (Activation)
├── Удержание (Retention)
├── Вовлечение (Engagement)
└── Удовлетворенность (Satisfaction)

УРОВЕНЬ 3: МОДЕЛЬНЫЕ МЕТРИКИ (Качество ML/LLM)
├── Качество ответов (Response Quality)
├── Скорость (Latency)
├── Эмпатия (Empathy)
├── Контекстность (Context Coherence)
└── Безопасность (Safety)
```

---

## 🏆 Бизнес-KPI #1: ВЫРУЧКА (Revenue)

### **Определение**: Деньги, заработанные от пользователей

### **Целевые значения**:
- **Q1 2025** (MVP): $0 (фокус на Product-Market Fit)
- **Q2 2025** (Monetization): $10,000 MRR (Monthly Recurring Revenue)
- **Q3 2025** (B2B Pivot): $50,000 MRR
- **Q4 2025** (Scale): $150,000 MRR

---

### 📊 Продукт-KPI #1.1: Конверсия в PRO-подписку (Free → Paid Conversion Rate)

**Определение**: % бесплатных пользователей, которые стали платными

**Целевое значение**: 5% (индустриальный стандарт для freemium: 2-5%)

**Формула**:
```
Conversion Rate = (Кол-во новых PRO подписчиков за период / Кол-во активных Free пользователей) × 100%
```

**Как измеряем**:
```sql
-- В database.py
SELECT 
    COUNT(DISTINCT user_id) FILTER (WHERE subscription_level = 'PRO') as pro_users,
    COUNT(DISTINCT user_id) FILTER (WHERE subscription_level = 'FREE' AND is_active = 1) as active_free_users,
    (pro_users::float / active_free_users * 100) as conversion_rate
FROM conversations
WHERE created_at >= NOW() - INTERVAL '30 days'
```

---

#### 🤖 Модельная метрика #1.1.1: Value Perception Score (Восприятие ценности)

**Определение**: Насколько пользователь осознает ценность продукта (по диалогам)

**Как работает**: LLM анализирует диалоги на предмет позитивных сигналов:
- Благодарности ("Спасибо, ты мне очень помогаешь!")
- Регулярное использование (5+ дней подряд)
- Глубокие беседы (10+ сообщений за сессию)

**Целевое значение**: > 0.70 (из 1.0)

**Как измеряем**:
```python
# В llm_service.py
VALUE_PERCEPTION_PROMPT = """
Проанализируй последние 10 сообщений пользователя:
{recent_messages}

Оцени "восприятие ценности" (0-1):
- 1.0: Явно ценит продукт ("Ты мне очень помогаешь", "Не знаю, что бы без тебя делал")
- 0.5-0.7: Умеренное удовлетворение (возвращается регулярно, но без явных комплиментов)
- 0-0.4: Низкое восприятие (редкие визиты, короткие диалоги)

Верни число от 0 до 1 с объяснением.
"""

async def calculate_value_perception(user_id: int) -> float:
    conversation = Conversation(user_id)
    await conversation.get_from_db()
    context = await conversation.get_context_for_llm()
    
    # Отправляем промпт в LLM
    score = await get_llm_value_perception(context)
    return score
```

**Связь с Conversion**:
- Value Perception > 0.8 → **Вероятность конверсии 20%**
- Value Perception 0.5-0.8 → **Вероятность конверсии 5%**
- Value Perception < 0.5 → **Вероятность конверсии 1%**

---

#### 🤖 Модельная метрика #1.1.2: Feature Request Detection (Обнаружение запросов функций)

**Определение**: Обнаружение запросов на функции PRO-плана

**Примеры**:
- "Жаль, что не можешь слушать голосовые"
- "Хотелось бы, чтобы ты помнил больше"
- "Можешь дать мне какие-то упражнения от стресса?"

**Целевое значение**: Обнаружить 80% запросов функций

**Как измеряем**:
```python
FEATURE_REQUEST_DETECTION_PROMPT = """
Сообщение пользователя: "{user_message}"

Задача: Определи, запрашивает ли пользователь функции, которых нет в Free версии:
- Голосовые сообщения
- Расширенный контекст (больше памяти)
- Персональные техники/упражнения
- Экспорт истории диалогов

Верни:
- "detected": true/false
- "feature": название функции (если detected)
- "confidence": 0-1 (уверенность)
"""

async def detect_feature_request(message_text: str) -> dict:
    result = await send_llm_detection_request(message_text)
    
    if result['detected'] and result['confidence'] > 0.7:
        # Триггер для показа PRO-предложения
        await trigger_pro_offer(user_id, feature=result['feature'])
    
    return result
```

**Связь с Conversion**:
- Пользователь запросил 2+ PRO-функции → **Конверсия ↑ на 40%**
- Показали релевантное предложение в течение 1 часа → **Конверсия ↑ на 25%**

---

#### 🤖 Модельная метрика #1.1.3: Pitch Personalization Accuracy (Точность персонализации предложения)

**Определение**: Насколько предложение PRO соответствует потребностям пользователя

**Как работает**: Сегментируем пользователей и подбираем pitch

**Сегменты**:
1. **Power Users** → pitch: "Безлимитный контекст"
2. **Emotional Support Seekers** → pitch: "Глубокий анализ эмоций, техники КПТ"
3. **Casual Users** → pitch: "7 дней бесплатно"

**Целевое значение**: 85% правильная сегментация

**Как измеряем**:
```python
# Human evaluation: раз в неделю
# Эксперт проверяет 100 предложений:
# - Соответствует ли pitch сегменту пользователя?
# - Релевантны ли упомянутые функции?

def evaluate_pitch_accuracy(sample_size=100):
    samples = get_random_pro_offers(sample_size)
    correct = 0
    
    for offer in samples:
        # Эксперт оценивает
        is_correct = expert_evaluation(offer)
        if is_correct:
            correct += 1
    
    return correct / sample_size
```

**Связь с Conversion**:
- Правильный pitch → **Конверсия ↑ на 50%** vs общее предложение
- Неправильный pitch → Может отпугнуть пользователя (-10% Retention)

---

### 📊 Продукт-KPI #1.2: Customer Lifetime Value (LTV)

**Определение**: Средняя выручка от одного пользователя за все время

**Целевое значение**: $100 (через 12 месяцев)

**Формула**:
```
LTV = ARPU (Average Revenue Per User) × Lifespan (в месяцах)

Пример:
ARPU = $8/месяц (средняя цена подписки)
Lifespan = 12 месяцев (средняя продолжительность подписки)
LTV = $8 × 12 = $96
```

**Как измеряем**:
```python
# В stats_service.py
def calculate_ltv():
    # Cohort analysis: пользователи, зарегистрированные в январе 2025
    jan_2025_users = get_users(registered_month='2025-01')
    
    total_revenue = sum([user.total_paid for user in jan_2025_users])
    total_users = len(jan_2025_users)
    
    ltv = total_revenue / total_users
    return ltv
```

---

#### 🤖 Модельная метрика #1.2.1: Response Quality (Качество ответов)

**Определение**: Насколько хороши ответы бота (оценка экспертов)

**Целевое значение**: > 4.2/5.0

**Как измеряем**:
```python
# Human Evaluation (раз в неделю)
# Критерии оценки (1-5):
criteria = {
    "empathy": "Проявлена ли эмпатия?",
    "relevance": "Релевантен ли ответ контексту?",
    "helpfulness": "Полезен ли ответ?",
    "naturalness": "Естественный ли язык?"
}

def human_eval_response_quality(sample_size=100):
    dialogs = get_random_dialogs(sample_size)
    scores = []
    
    for dialog in dialogs:
        expert_score = expert_rate(dialog, criteria)
        scores.append(expert_score)
    
    return sum(scores) / len(scores)  # Средний балл
```

**Связь с LTV**:
- Response Quality > 4.5 → **LTV ↑ на 30%** (пользователи остаются дольше)
- Response Quality < 3.5 → **Churn Rate ↑ на 50%**

---

#### 🤖 Модельная метрика #1.2.2: Session Length (Длительность сессии)

**Определение**: Среднее время одной беседы

**Целевое значение**: > 15 минут

**Как измеряем**:
```python
# В database.py
async def calculate_avg_session_length(user_id: int) -> float:
    """Возвращает среднюю длительность сессии в минутах"""
    async with aiosqlite.connect(DATABASE_NAME) as db:
        cursor = await db.execute(
            """
            SELECT 
                AVG(julianday(session_end) - julianday(session_start)) * 24 * 60 as avg_minutes
            FROM (
                SELECT 
                    MIN(timestamp) as session_start,
                    MAX(timestamp) as session_end
                FROM messages
                WHERE user_id = ?
                GROUP BY DATE(timestamp)
            )
            """,
            (user_id,)
        )
        result = await cursor.fetchone()
        return result[0] if result[0] else 0
```

**Связь с LTV**:
- Session Length > 20 минут → **LTV ↑ на 40%** (высокое вовлечение)
- Session Length < 5 минут → **LTV ↓ на 50%** (поверхностное использование)

---

### 📊 Продукт-KPI #1.3: Customer Acquisition Cost (CAC)

**Определение**: Средняя стоимость привлечения одного пользователя

**Целевое значение**: < $5 (B2C), < $500 (B2B)

**Формула**:
```
CAC = Расходы на маркетинг / Кол-во новых пользователей

Пример (B2C):
Расходы на рекламу в соцсетях: $1000/месяц
Новые пользователи: 200/месяц
CAC = $1000 / 200 = $5
```

**Требование**: **LTV/CAC > 3:1** (индустриальный стандарт)

**Как измеряем**:
```python
# В analytics dashboard
def calculate_cac():
    marketing_spend = get_monthly_marketing_spend()  # Из рекламных кабинетов
    new_users = count_new_users_this_month()
    
    cac = marketing_spend / new_users
    
    ltv = calculate_ltv()
    ltv_cac_ratio = ltv / cac
    
    return {
        "cac": cac,
        "ltv": ltv,
        "ratio": ltv_cac_ratio,
        "healthy": ltv_cac_ratio > 3
    }
```

---

#### 🤖 Модельная метрика #1.3.1: Viral Coefficient (Виральность)

**Определение**: Сколько новых пользователей привлекает один существующий

**Целевое значение**: > 0.5 (каждый пользователь приводит 0.5 друзей → органический рост)

**Формула**:
```
K = (% пользователей, сделавших реферал) × (среднее кол-во рефералов на пользователя)

Пример:
10% пользователей делятся ботом
Каждый приводит в среднем 3 друга
K = 0.10 × 3 = 0.3
```

**Как измеряем**:
```python
# В database.py (реферальная система)
async def calculate_viral_coefficient():
    total_users = await count_total_users()
    
    # Пользователи, которые привели хотя бы одного друга
    referring_users = await db.execute(
        """
        SELECT COUNT(DISTINCT referrer_id)
        FROM conversations
        WHERE referral_code IS NOT NULL
        """
    )
    
    # Общее количество рефералов
    total_referrals = await db.execute(
        """
        SELECT COUNT(*)
        FROM conversations
        WHERE referral_code IS NOT NULL
        """
    )
    
    referral_rate = referring_users / total_users
    avg_referrals = total_referrals / referring_users if referring_users > 0 else 0
    
    k = referral_rate * avg_referrals
    return k
```

**Связь с CAC**:
- K > 1.0 → **Экспоненциальный рост** (каждый пользователь приводит >1 друга)
- K = 0.5 → **CAC ↓ на 33%** (1/3 пользователей приходит органически)

---

#### 🤖 Модельная метрика #1.3.2: Onboarding Conversion Rate (Конверсия после первой сессии)

**Определение**: % пользователей, отправивших >3 сообщений в первой сессии

**Целевое значение**: > 60%

**Как измеряем**:
```python
# В stats_service.py
async def calculate_onboarding_conversion():
    new_users = await get_new_users_this_week()
    converted = 0
    
    for user in new_users:
        first_session_messages = await get_first_session_messages(user.id)
        if len(first_session_messages) >= 3:
            converted += 1
    
    return converted / len(new_users) if new_users else 0
```

**Связь с CAC**:
- Onboarding Conversion > 70% → **Эффективность маркетинга ↑** (трафик качественный)
- Onboarding Conversion < 40% → **CAC фактически в 2х** (половина трафика бесполезна)

---

## 🏆 Бизнес-KPI #2: ПРИБЫЛЬНОСТЬ (Profitability)

### **Определение**: Прибыль = Выручка - Расходы

### **Целевые значения**:
- **Q2 2025**: Break-even (прибыль = 0)
- **Q3 2025**: 30% маржа
- **Q4 2025**: 50% маржа

### **Основные расходы**:
1. **Инфраструктура**: Серверы, БД ($100-500/мес)
2. **LLM API**: OpenRouter (Gemini 2.0) — основная статья расходов
3. **Маркетинг**: Реклама, PR ($1000-5000/мес)
4. **Команда**: Разработка, поддержка (зависит от стадии)

---

### 📊 Продукт-KPI #2.1: Cost per Interaction (Стоимость одного взаимодействия)

**Определение**: Сколько стоит один запрос к LLM API

**Целевое значение**: < $0.02 за взаимодействие

**Формула**:
```
Cost per Interaction = API Costs / Total Interactions

Пример:
Gemini 2.0 Flash: $0.075 per 1M input tokens, $0.30 per 1M output tokens
Средний промпт: 500 input tokens + 200 output tokens
Cost = (500 × $0.075 / 1M) + (200 × $0.30 / 1M) = $0.0000375 + $0.00006 = $0.0000975 ≈ $0.0001

При 10,000 взаимодействий/день:
Daily cost = 10,000 × $0.0001 = $1/день = $30/месяц ✅
```

**Как измеряем**:
```python
# В llm_client.py (логирование usage)
async def send_request_to_openrouter(prompt):
    response = await api_call(prompt)
    
    # Логируем использование токенов
    usage = response['usage']
    await log_api_usage(
        input_tokens=usage['prompt_tokens'],
        output_tokens=usage['completion_tokens'],
        model="google/gemini-2.0-flash-exp",
        cost=calculate_cost(usage)
    )
    
    return response['choices'][0]['message']['content']
```

---

#### 🤖 Модельная метрика #2.1.1: Token Efficiency (Эффективность использования токенов)

**Определение**: Отношение полезных токенов к общим

**Целевое значение**: > 0.80 (80% токенов несут ценность)

**Как работает**:
- **Избыточные токены**: Повторяющийся контекст, ненужные пояснения
- **Полезные токены**: Уникальный контент, релевантный ответ

**Оптимизация**:
```python
# До оптимизации
prompt = {
    "system": "Ты общительный ассистент который активно интересуется жизнью пользователя...",
    "context": [  # Последние 10 сообщений, включая повторы
        {"role": "user", "content": "Привет"},
        {"role": "assistant", "content": "Привет! Как дела?"},
        # ... 8 сообщений
    ],
    "current": "Как дела?"
}
# Итого: 800 токенов

# После оптимизации
prompt = {
    "system": "Ты Эдя, AI-друг. Стиль: короткий, дружелюбный.",  # Сокращенный промпт
    "context": [  # Только уникальные/важные сообщения (5 последних)
        {"role": "user", "content": "Готовлюсь к экзамену"},
        {"role": "assistant", "content": "Удачи! Что сложнее всего?"},
        # ... 3 сообщения
    ],
    "current": "Как дела?"
}
# Итого: 400 токенов (↓50% → ↓50% стоимость)
```

**Как измеряем**:
```python
def calculate_token_efficiency():
    # Анализируем 100 случайных промптов
    samples = get_random_prompts(100)
    
    for prompt in samples:
        total_tokens = count_tokens(prompt)
        unique_tokens = count_unique_tokens(prompt)
        
        efficiency = unique_tokens / total_tokens
    
    return avg(efficiencies)
```

**Связь с Cost**:
- Token Efficiency ↑ 10% → **Cost per Interaction ↓ 10%**

---

#### 🤖 Модельная метрика #2.1.2: Response Length Optimization (Оптимизация длины ответов)

**Определение**: Средняя длина ответа LLM (в токенах)

**Целевое значение**: 50-150 токенов (короткие, дружелюбные реплики)

**Проблема**: По умолчанию GPT-4 пишет длинно (200-300 токенов)

**Решение**: Промпт "Будь лаконичным, 1-2 предложения"

**Как измеряем**:
```python
# В llm_client.py
async def log_response_length(response_text: str):
    token_count = count_tokens(response_text)
    await db.execute(
        "INSERT INTO response_metrics (timestamp, token_count) VALUES (?, ?)",
        (datetime.now(), token_count)
    )
```

**Связь с Cost**:
- Avg Response Length = 100 токенов → $0.00003 за ответ
- Avg Response Length = 200 токенов → $0.00006 за ответ (**↑100% стоимость**)

---

### 📊 Продукт-KPI #2.2: Infrastructure Cost per User (Инфраструктурные расходы на пользователя)

**Определение**: Стоимость серверов, БД на одного активного пользователя

**Целевое значение**: < $0.50/месяц на пользователя

**Формула**:
```
Infrastructure Cost per User = (Серверы + БД + CDN) / Monthly Active Users

Пример:
Инфраструктура: $500/месяц
MAU: 5000
Cost per User = $500 / 5000 = $0.10/месяц ✅
```

**Как измеряем**:
```python
# В финансовом dashboard
def calculate_infra_cost_per_user():
    # Из AWS/Yandex Cloud billing
    server_cost = get_monthly_server_cost()  # $300
    db_cost = get_monthly_db_cost()  # $100
    cdn_cost = get_monthly_cdn_cost()  # $50
    
    total_infra_cost = server_cost + db_cost + cdn_cost  # $450
    
    mau = count_monthly_active_users()  # 5000
    
    return total_infra_cost / mau  # $0.09
```

---

## 🏆 Бизнес-KPI #3: РОСТ (Growth)

### **Определение**: Темп роста пользовательской базы

### **Целевые значения**:
- **Q1 2025**: 1000 активных пользователей
- **Q2 2025**: 5000 активных пользователей (+400% MoM)
- **Q3 2025**: 20,000 активных пользователей (+300% MoM)
- **Q4 2025**: 50,000+ активных пользователей (B2B масштабирование)

---

### 📊 Продукт-KPI #3.1: Monthly Active Users (MAU)

**Определение**: Количество уникальных пользователей, отправивших хотя бы 1 сообщение за месяц

**Целевое значение**: 5000 к Q2 2025

**Как измеряем**:
```python
# В stats_service.py
async def calculate_mau():
    async with aiosqlite.connect(DATABASE_NAME) as db:
        cursor = await db.execute(
            """
            SELECT COUNT(DISTINCT user_id)
            FROM messages
            WHERE timestamp >= DATE('now', '-30 days')
            """
        )
        result = await cursor.fetchone()
        return result[0]
```

---

#### 🤖 Модельная метрика #3.1.1: 7-Day Retention Rate

**Определение**: % пользователей, вернувшихся через 7 дней после регистрации

**Целевое значение**: > 40%

**Формула**:
```
7-Day Retention = (Пользователи, активные на день 7) / (Всего зарегистрировано на день 0) × 100%
```

**Как измеряем**:
```python
# Cohort analysis
async def calculate_7day_retention():
    # Пользователи, зарегистрированные 7 дней назад
    cohort_date = datetime.now() - timedelta(days=7)
    cohort_users = await get_users_registered_on(cohort_date)
    
    # Сколько из них активны сегодня?
    active_today = 0
    for user in cohort_users:
        if await is_user_active_today(user.id):
            active_today += 1
    
    return (active_today / len(cohort_users)) * 100 if cohort_users else 0
```

**Связь с MAU**:
- 7-Day Retention > 50% → **Устойчивый рост MAU** (пользователи остаются)
- 7-Day Retention < 30% → **MAU стагнирует** (отток компенсирует приток)

---

#### 🤖 Модельная метрика #3.1.2: Reminder Effectiveness (Эффективность напоминаний)

**Определение**: % пользователей, вернувшихся после напоминания

**Целевое значение**: > 30%

**Формула**:
```
Reminder Response Rate = (Пользователи, ответившие на напоминание) / (Всего напоминаний отправлено) × 100%
```

**Как измеряем**:
```python
# В reminder_service.py
async def calculate_reminder_effectiveness():
    # Напоминания за последние 7 дней
    reminders_sent = await db.execute(
        """
        SELECT user_id, timestamp
        FROM reminders
        WHERE timestamp >= DATE('now', '-7 days')
        """
    )
    
    responses = 0
    for reminder in reminders_sent:
        user_id, reminder_time = reminder
        
        # Проверяем, был ли ответ в течение 24 часов после напоминания
        replied = await db.execute(
            """
            SELECT COUNT(*)
            FROM messages
            WHERE user_id = ? 
            AND role = 'user'
            AND timestamp BETWEEN ? AND ?
            """,
            (user_id, reminder_time, reminder_time + timedelta(hours=24))
        )
        
        if replied[0] > 0:
            responses += 1
    
    return (responses / len(reminders_sent)) * 100 if reminders_sent else 0
```

**Связь с Retention**:
- Reminder Effectiveness > 40% → **7-Day Retention ↑ на 20%**
- Без напоминаний → **7-Day Retention ↓ на 35%**

---

### 📊 Продукт-KPI #3.2: Net Promoter Score (NPS)

**Определение**: Готовность пользователей рекомендовать продукт

**Целевое значение**: > 50 (индустриальный стандарт для B2C: 30-50)

**Формула**:
```
NPS = % Промоутеров (9-10) - % Критиков (0-6)

Вопрос: "Насколько вероятно, что вы порекомендуете Эдю другу? (0-10)"
- Промоутеры: 9-10
- Пассивы: 7-8
- Критики: 0-6
```

**Как измеряем**:
```python
# Периодический опрос (раз в месяц)
async def send_nps_survey():
    active_users = await get_active_users_this_month()
    
    for user in active_users:
        # Отправляем опрос через бота
        await bot.send_message(
            user.id,
            "Привет! Быстрый вопрос: насколько вероятно (0-10), что ты порекомендуешь меня другу? 😊"
        )
        
        # Сохраняем ответ
        response = await wait_for_user_response(user.id)
        await save_nps_score(user.id, int(response))

def calculate_nps():
    scores = get_all_nps_scores()
    
    promoters = len([s for s in scores if s >= 9])
    detractors = len([s for s in scores if s <= 6])
    
    nps = ((promoters - detractors) / len(scores)) * 100
    return nps
```

---

#### 🤖 Модельная метрика #3.2.1: Sentiment Analysis (Анализ тональности)

**Определение**: Средняя тональность сообщений пользователей (позитивная/негативная)

**Целевое значение**: > 0.60 (из 1.0 — позитивная тональность)

**Как работает**:
```python
SENTIMENT_ANALYSIS_PROMPT = """
Проанализируй тональность сообщения пользователя:
"{user_message}"

Верни sentiment score (-1 до +1):
- +1: Очень позитивное ("Спасибо, ты мне очень помог!", "Ты лучший!")
- 0: Нейтральное ("Привет", "Как дела?")
- -1: Очень негативное ("Ты бесполезен", "Мне хуже")
"""

async def analyze_message_sentiment(message_text: str) -> float:
    score = await get_llm_sentiment(message_text)
    
    # Сохраняем для аналитики
    await db.execute(
        "INSERT INTO sentiment_scores (message_id, score, timestamp) VALUES (?, ?, ?)",
        (message_id, score, datetime.now())
    )
    
    return score
```

**Связь с NPS**:
- Avg Sentiment > 0.7 → **NPS > 60** (довольные пользователи)
- Avg Sentiment < 0.3 → **NPS < 20** (недовольные пользователи)

---

#### 🤖 Модельная метрика #3.2.2: Empathy Score (Эмпатия)

**Определение**: Насколько ответы бота эмпатичны (оценка экспертов)

**Целевое значение**: > 0.80 (из 1.0)

**Как измеряем**:
```python
# Human Evaluation
EMPATHY_CRITERIA = """
Оцени эмпатию ответа бота (0-1):

0.9-1.0: Высокая эмпатия
- Признание эмоций пользователя
- Validation ("Понимаю, это действительно тяжело")
- Поддержка и забота

0.5-0.8: Умеренная эмпатия
- Понимание ситуации
- Нейтральная поддержка

0-0.4: Низкая эмпатия
- Игнорирование эмоций
- Формальный ответ
- Неуместные советы
"""

def evaluate_empathy(sample_size=100):
    dialogs = get_random_dialogs(sample_size)
    scores = []
    
    for dialog in dialogs:
        expert_score = expert_rate_empathy(dialog)
        scores.append(expert_score)
    
    return sum(scores) / len(scores)
```

**Связь с NPS**:
- Empathy Score > 0.85 → **NPS > 55**
- Empathy Score < 0.65 → **NPS < 30**

---

## 📋 Итоговая таблица: Полная карта метрик

| Бизнес-KPI | Целевое значение | Продукт-KPI | Целевое значение | Модельная метрика | Целевое значение | Как измеряем |
|-----------|-----------------|------------|-----------------|------------------|-----------------|-------------|
| **Выручка (Revenue)** | $50K MRR к Q3 2025 | | | | | |
| | | Конверсия Free→PRO | 5% | Value Perception Score | > 0.70 | Анализ удовлетворенности |
| | | | | Feature Request Detection | 80% точность | LLM обнаружение запросов |
| | | | | Pitch Personalization | 85% точность | Human eval сегментации |
| | | LTV (Customer Lifetime Value) | $100 | Response Quality | > 4.2/5 | Human eval (1-5 звезд) |
| | | | | Session Length | > 15 мин | Timestamp анализ |
| | | CAC (Customer Acquisition Cost) | < $5 (B2C) | Viral Coefficient | > 0.5 | Реферальная система |
| | | | | Onboarding Conversion | > 60% | % отправивших >3 сообщения |
| **Прибыльность (Profitability)** | 30% маржа к Q3 2025 | | | | | |
| | | Cost per Interaction | < $0.02 | Token Efficiency | > 0.80 | Unique tokens / Total tokens |
| | | | | Response Length | 50-150 токенов | Avg токенов на ответ |
| | | Infra Cost per User | < $0.50/мес | Latency (Скорость) | < 3 сек | P99 время ответа |
| **Рост (Growth)** | 20K MAU к Q3 2025 | | | | | |
| | | MAU (Monthly Active Users) | 5K к Q2 2025 | 7-Day Retention | > 40% | Cohort analysis |
| | | | | Reminder Effectiveness | > 30% | % ответивших на напоминание |
| | | NPS (Net Promoter Score) | > 50 | Sentiment Analysis | > 0.60 | LLM анализ тональности |
| | | | | Empathy Score | > 0.80 | Human eval эмпатии |

---

## 📊 Дашборд метрик (рекомендуемая визуализация)

### **Реализация**: Grafana / Metabase / Custom Dashboard

#### **Панель 1: Бизнес-здоровье (Executive Dashboard)**
```
┌─────────────────────────────────────────────────────┐
│  MRR: $15,000  (+25% MoM) 📈                        │
│  LTV/CAC: 4.2  (✅ Healthy)                          │
│  Margin: 28%  (✅ On Track)                          │
└─────────────────────────────────────────────────────┘
```

#### **Панель 2: Пользовательские метрики**
```
┌─────────────────────────────────────────────────────┐
│  MAU: 4,500  (+35% MoM) 📈                          │
│  7-Day Retention: 42%  (✅)                          │
│  NPS: 52  (✅)                                       │
│  Free→PRO Conversion: 4.8%  (⚠️ Близко к цели)     │
└─────────────────────────────────────────────────────┘
```

#### **Панель 3: Качество модели**
```
┌─────────────────────────────────────────────────────┐
│  Response Quality: 4.4/5  (✅)                       │
│  Empathy Score: 0.82  (✅)                           │
│  Latency (P99): 2.8s  (✅)                           │
│  Token Efficiency: 0.78  (⚠️ Optimize)              │
└─────────────────────────────────────────────────────┘
```

---

## 🔍 Риски неверной оптимизации

### **Антипаттерн #1: Оптимизация ТОЛЬКО модельных метрик**

**Пример**:
- **Цель**: Повысить Response Quality с 4.2 до 4.8
- **Действие**: LLM начинает писать длинные, развернутые ответы (300+ токенов)
- **Результат**:
  - ✅ Response Quality = 4.8 (цель достигнута)
  - ❌ Cost per Interaction ↑ на 100% (убытки)
  - ❌ Latency ↑ с 3 сек до 7 сек (пользователи раздражены)
  - ❌ Session Length ↓ (пользователи уходят от длинных ответов)

**Урок**: Оптимизировать нужно **комплексно**, отслеживая побочные эффекты.

---

### **Антипаттерн #2: Оптимизация косвенных метрик без учета бизнес-цели**

**Пример**:
- **Цель**: Повысить 7-Day Retention
- **Действие**: Отправлять напоминания каждый день (вместо раз в 2 дня)
- **Результат**:
  - ✅ 7-Day Retention ↑ с 40% до 45% (краткосрочно)
  - ❌ NPS ↓ с 52 до 35 (пользователи раздражены)
  - ❌ Churn Rate ↑ на 20% (долгосрочно пользователи уходят)
  - ❌ Блокировки бота ↑ на 50%

**Урок**: Всегда проверять влияние на **NPS и Churn** при оптимизации Retention.

---

### **Антипаттерн #3: Игнорирование стоимости**

**Пример**:
- **Цель**: Улучшить качество ответов
- **Действие**: Переход с Gemini 2.0 Flash на GPT-4 (в 10х дороже)
- **Результат**:
  - ✅ Response Quality ↑ с 4.2 до 4.6
  - ❌ Cost per Interaction ↑ с $0.0001 до $0.001 (в 10х!)
  - ❌ Monthly API Cost ↑ с $300 до $3000 (компания убыточна)

**Урок**: Оптимизировать **LTV/CAC** важнее, чем абсолютное качество.

---

## ✅ Best Practices

1. **Связывайте метрики причинно-следственно**
   - Empathy ↑ → Session Length ↑ → Retention ↑ → LTV ↑ → Revenue ↑

2. **A/B тестируйте изменения**
   - Группа A (контроль) vs Группа B (новый промпт)
   - Измеряйте влияние на ВСЕ уровни метрик (не только модельные)

3. **Мониторинг в реальном времени**
   - Алерты: Latency > 5 сек → Telegram уведомление админам
   - Алерты: Cost per Day > $100 → Проверить аномалии

4. **Еженедельные ретроспективы**
   - Что улучшилось?
   - Какие метрики ухудшились?
   - Какие гипотезы тестировать дальше?

