# 🚀 Руководство по развертыванию Empathy AI Bot

Полное руководство по установке и запуску бота различными способами.

## 📋 Содержание

- [Предварительные требования](#предварительные-требования)
- [Метод 1: Локальный запуск (Python)](#метод-1-локальный-запуск-python)
- [Метод 2: Docker](#метод-2-docker)
- [Метод 3: Docker Compose (рекомендуется)](#метод-3-docker-compose-рекомендуется)
- [Настройка переменных окружения](#настройка-переменных-окружения)
- [Управление ботом](#управление-ботом)
- [Решение проблем](#решение-проблем)

---

## Предварительные требования

### Для всех методов:
- Токен Telegram бота от [@BotFather](https://t.me/BotFather)
- API ключ от [OpenRouter](https://openrouter.ai/)
- Ваш Telegram chat ID (получите через [@userinfobot](https://t.me/userinfobot))

### Для локального запуска:
- Python 3.14+ (или 3.10+)
- pip

### Для Docker:
- Docker 20.10+
- Docker Compose 2.0+ (опционально, но рекомендуется)

---

## Метод 1: Локальный запуск (Python)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd empathy-ai-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Активация на macOS/Linux:
source venv/bin/activate

# Активация на Windows:
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и заполните необходимые значения:

```bash
nano .env  # или используйте любой текстовый редактор
```

### 5. Запуск бота

```bash
python main.py
```

Вывод в консоли:

```
Бд подгружена успешно
Основная часть запущена
Нажмите Ctrl-C для остановки бота
```

### 6. Остановка бота

Нажмите **Ctrl-C** в терминале.

Если бот не останавливается, используйте:

```bash
./kill_bot.sh
```

---

## Метод 2: Docker

### 1. Подготовка

Убедитесь, что у вас установлен Docker:

```bash
docker --version
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # Заполните необходимые значения
```

### 3. Сборка образа

```bash
docker build -t empathy-ai-bot .
```

### 4. Запуск контейнера

```bash
docker run -d \
  --name empathy-bot \
  --env-file .env \
  -v $(pwd)/data:/data \
  -v $(pwd)/debug.log:/app/debug.log \
  --restart unless-stopped \
  empathy-ai-bot
```

### 5. Управление контейнером

**Просмотр логов:**

```bash
docker logs -f empathy-bot
```

**Остановка:**

```bash
docker stop empathy-bot
```

**Перезапуск:**

```bash
docker restart empathy-bot
```

**Удаление контейнера:**

```bash
docker rm -f empathy-bot
```

---

## Метод 3: Docker Compose (рекомендуется)

Самый простой и удобный способ для production.

### 1. Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # Заполните необходимые значения
```

### 2. Запуск

```bash
docker-compose up -d
```

Флаги:
- `-d` — запуск в фоновом режиме

### 3. Просмотр логов

```bash
# Все логи:
docker-compose logs -f

# Последние 100 строк:
docker-compose logs --tail=100 -f
```

### 4. Остановка

```bash
docker-compose down
```

### 5. Перезапуск

```bash
docker-compose restart
```

### 6. Пересборка и перезапуск

Если изменили код:

```bash
docker-compose down
docker-compose up -d --build
```

### 7. Полная очистка

Удаление контейнера, образа и volumes:

```bash
docker-compose down -v
docker rmi empathy-ai-bot
```

---

## Настройка переменных окружения

### Обязательные параметры

```env
# Telegram Bot
TG_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# OpenRouter API
LLM_TOKEN=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MODEL=google/gemini-2.0-flash-exp:free

# Debug
DEBUG=0
DEBUG_CHAT=123456789

# Database
DATABASE_NAME=users.db
TABLE_NAME=users
MAX_CONTEXT=20

# Reminders
DELAYED_REMINDERS_HOURS=2
DELAYED_REMINDERS_MINUTES=0
TIMEZONE_OFFSET=3
FROM_TIME=9
TO_TIME=23

# Admin (опционально)
ADMIN_LIST=
```

### Рекомендуемые модели LLM

| Модель | Тип | Описание |
|--------|-----|----------|
| `google/gemini-2.0-flash-exp:free` | Бесплатная | Быстрая, хорошее качество |
| `openai/gpt-4o-mini` | Платная | Отличное качество, доступная цена |
| `anthropic/claude-3.5-sonnet` | Платная | Высочайшее качество |

Полный список: https://openrouter.ai/models

---

## Управление ботом

### Проверка статуса

**Локально:**

```bash
ps aux | grep "python main.py"
```

**Docker:**

```bash
docker ps | grep empathy
```

**Docker Compose:**

```bash
docker-compose ps
```

### Просмотр логов

**Локально:**

```bash
tail -f debug.log
```

**Docker:**

```bash
docker logs -f empathy-bot
```

**Docker Compose:**

```bash
docker-compose logs -f
```

### Проверка здоровья контейнера

```bash
docker inspect --format='{{.State.Health.Status}}' empathy-bot
```

---

## Решение проблем

### Бот не отвечает на сообщения

1. Проверьте правильность `TG_TOKEN`:
   ```bash
   cat .env | grep TG_TOKEN
   ```

2. Убедитесь, что бот запущен:
   ```bash
   # Локально:
   ps aux | grep "python main.py"
   
   # Docker:
   docker ps | grep empathy
   ```

3. Проверьте логи:
   ```bash
   # Локально:
   tail -50 debug.log
   
   # Docker:
   docker logs --tail=50 empathy-bot
   ```

### Ошибки LLM

1. Проверьте баланс на OpenRouter: https://openrouter.ai/

2. Проверьте токен:
   ```bash
   cat .env | grep LLM_TOKEN
   ```

3. Проверьте доступность модели:
   ```bash
   cat .env | grep MODEL
   ```

### Проблемы с базой данных

**Локально:**

```bash
# Проверьте права доступа:
ls -la *.db

# Пересоздайте базу:
rm users.db
python main.py
```

**Docker:**

```bash
# Проверьте volume:
docker volume ls

# Пересоздайте volume:
docker-compose down -v
docker-compose up -d
```

### Контейнер постоянно перезапускается

1. Проверьте логи:
   ```bash
   docker logs empathy-bot
   ```

2. Проверьте переменные окружения:
   ```bash
   docker exec empathy-bot env | grep -E 'TG_TOKEN|LLM_TOKEN'
   ```

3. Запустите в интерактивном режиме для отладки:
   ```bash
   docker run -it --rm --env-file .env empathy-ai-bot
   ```

### Бот не останавливается по Ctrl-C (локально)

Используйте скрипт принудительной остановки:

```bash
./kill_bot.sh
```

### Проблемы с портами (если расширяете функционал)

Если добавляете веб-интерфейс или API, не забудьте пробросить порты в `docker-compose.yml`:

```yaml
ports:
  - "8000:8000"
```

---

## Рекомендации для Production

### 1. Используйте Docker Compose

Самый простой способ управления в production.

### 2. Настройте автозапуск

В `docker-compose.yml` уже настроен `restart: unless-stopped`.

### 3. Мониторинг логов

Настройте ротацию логов:

```yaml
logging:
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"
```

### 4. Бэкапы базы данных

Регулярно копируйте `data/users.db`:

```bash
# Создайте cron задачу:
0 3 * * * cp /path/to/empathy-ai-bot/data/users.db /backups/users-$(date +\%Y\%m\%d).db
```

### 5. Безопасность

- Никогда не коммитьте `.env`
- Используйте сильные токены
- Регулярно обновляйте зависимости:
  ```bash
  pip list --outdated
  docker-compose pull
  ```

### 6. Мониторинг

Добавьте healthcheck в Docker Compose (уже настроен) и интегрируйте с системой мониторинга (Prometheus, Grafana, Uptime Kuma).

---

## Дополнительные команды

### Обновление кода

**Локально:**

```bash
git pull
pip install -r requirements.txt
# Перезапустите бота
```

**Docker Compose:**

```bash
git pull
docker-compose down
docker-compose up -d --build
```

### Очистка старых образов Docker

```bash
docker system prune -a
```

### Экспорт/импорт базы данных

**Экспорт:**

```bash
sqlite3 data/users.db .dump > backup.sql
```

**Импорт:**

```bash
sqlite3 data/users.db < backup.sql
```

---

## Полезные ссылки

- [Документация Docker](https://docs.docker.com/)
- [Документация Docker Compose](https://docs.docker.com/compose/)
- [Документация aiogram](https://docs.aiogram.dev/)
- [OpenRouter API](https://openrouter.ai/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

Если возникли вопросы или проблемы, создайте Issue в репозитории проекта.

