# Умное исправление Markdown

## Обзор

Система умного исправления Markdown автоматически обрабатывает ошибки форматирования в сообщениях Telegram, используя информацию из сообщений об ошибках API.

## Как это работает

### 1. Парсинг ошибок Telegram

Когда Telegram возвращает ошибку парсинга, например:
```
Bad Request: can't parse entities: Can't find end of Underline entity at byte offset 487
```

Функция `parse_telegram_error()` извлекает:
- **Тип entity**: `Underline` → символ `__`
- **Позицию ошибки**: `byte offset 487`

Поддерживаемые типы entity:
- **Underline** → `__` (подчеркивание)
- **Bold** → `*` (жирный)
- **Italic** → `_` (курсив)
- **Strikethrough** → `~` (зачеркнутый)
- **Code** → `` ` `` (моноширинный)
- **Spoiler** → `||` (спойлер)

### 2. Целенаправленное исправление

Функция `fix_markdown_at_offset()` работает по следующему алгоритму:

#### Шаг 1: Конвертация byte offset в character offset
- Учитывает UTF-8 кодировку (кириллица, эмодзи занимают несколько байт)
- Находит точную позицию проблемного символа

#### Шаг 2: Анализ всех вхождений проблемного символа
Находит все вхождения символа в тексте и определяет их тип:

**Открывающий символ** (например `_привет`):
- После пробела/начала строки/скобки `([{`
- Перед не-пробелом

**Закрывающий символ** (например `привет_`):
- После не-пробела
- Перед пробелом/концом строки/знаком препинания `.!?,;:)]}`

#### Шаг 3: Составление пар
- Для каждого открывающего символа ищется соответствующий закрывающий
- Формируются пары валидных markdown тегов

#### Шаг 4: Поиск непарных символов
- Определяются символы без пары
- Выбирается непарный символ ближайший к позиции ошибки
- Символ экранируется добавлением `\` перед ним

### 3. Fallback механизм

Система использует многоуровневую стратегию исправления:

```python
try:
    # 1. Отправка с оригинальным markdown
    bot.send_message(text, parse_mode=MarkdownV2)
except TelegramBadRequest as e:
    if "can't parse entities" in str(e):
        # 2. Целенаправленное исправление по ошибке
        char, offset = parse_telegram_error(str(e))
        fixed = fix_markdown_at_offset(text, char, offset)
        
        try:
            bot.send_message(fixed, parse_mode=MarkdownV2)
        except:
            # 3. Общее исправление markdown
            fixed = fix_nested_markdown(fixed)
            
            try:
                bot.send_message(fixed, parse_mode=MarkdownV2)
            except:
                # 4. Отправка без форматирования
                bot.send_message(text, parse_mode=None)
```

## Примеры

### Пример 1: Непарное подчеркивание

**Проблема:**
```
Текст с _непарным подчеркиванием
```

**Ошибка Telegram:**
```
Can't find end of Italic entity at byte offset 10
```

**Исправление:**
```
Текст с \_непарным подчеркиванием
```

### Пример 2: Множественные теги

**Проблема:**
```
_первый тег_ _второй тег _третий непарный
```

**Алгоритм:**
1. Находит все `_` в позициях: 0, 11, 13, 24, 32
2. Определяет пары: (0, 11), (13, 24)
3. Непарный: 32
4. Экранирует: `_первый тег_ _второй тег \_третий непарный`

### Пример 3: UTF-8 символы

**Проблема:**
```
Привет 🤔 текст с _непарным
```

**Особенность:**
- "Привет " = 7 символов, ~14 байт в UTF-8
- 🤔 = 1 символ, 4 байта в UTF-8
- Byte offset учитывает реальное количество байт

## Преимущества

### До улучшения
- Общее исправление всех markdown тегов
- Могли экранироваться валидные теги
- Неточное определение проблемных символов

### После улучшения
- ✅ Точное определение проблемного символа
- ✅ Целенаправленное исправление только непарных тегов
- ✅ Сохранение валидных markdown тегов
- ✅ Корректная работа с UTF-8 (кириллица, эмодзи)
- ✅ Учет правил открывающих/закрывающих символов

## Тестирование

Функциональность покрыта комплексными тестами:

```bash
pytest tests/test_telegram_error_parsing.py -v
```

**Тестовые сценарии:**
- ✅ Парсинг различных типов ошибок Telegram
- ✅ Исправление непарных символов
- ✅ Корректная работа с UTF-8
- ✅ Работа с эмодзи
- ✅ Множественные теги
- ✅ Правила открывающих/закрывающих символов
- ✅ Интеграционные тесты

Всего: **26 тестов**, все проходят успешно.

## API

### `parse_telegram_error(error_message: str) -> tuple[str | None, int | None]`

Парсит сообщение об ошибке от Telegram API.

**Параметры:**
- `error_message` - текст ошибки от Telegram

**Возвращает:**
- Кортеж `(символ, byte_offset)` или `(None, None)` если не удалось распарсить

**Пример:**
```python
error = "Can't find end of Underline entity at byte offset 487"
char, offset = parse_telegram_error(error)
# char = '__', offset = 487
```

### `fix_markdown_at_offset(text: str, problem_char: str, byte_offset: int) -> str`

Исправляет конкретный проблемный символ markdown.

**Параметры:**
- `text` - исходный текст
- `problem_char` - проблемный символ (`_`, `__`, `*`, и т.д.)
- `byte_offset` - позиция ошибки в байтах

**Возвращает:**
- Исправленный текст с экранированным непарным символом

**Пример:**
```python
text = "Текст с _непарным символом"
fixed = fix_markdown_at_offset(text, '_', 10)
# fixed = "Текст с \\_непарным символом"
```

### `fix_nested_markdown(text: str) -> str`

Общее исправление вложенных markdown тегов (fallback).

**Параметры:**
- `text` - текст с потенциально некорректным markdown

**Возвращает:**
- Исправленный текст

## Логирование

Система логирует все этапы исправления:

```
WARNING - CHAT123456 - ошибка парсинга Markdown: Can't find end of Underline entity at byte offset 487
INFO - CHAT123456 - обнаружен проблемный символ '__' на позиции 487. Пробуем целенаправленное исправление...
```

При неудачном исправлении:

```
WARNING - CHAT123456 - целенаправленное исправление не помогло полностью. Пробуем общее исправление...
WARNING - CHAT123456 - исправление не помогло. Отправляем без форматирования.
```

## Дальнейшее развитие

Возможные улучшения:
- [ ] Кэширование исправленных текстов
- [ ] Статистика типов ошибок
- [ ] Предупреждения для пользователей о некорректном форматировании
- [ ] Поддержка других типов ошибок парсинга

